name: Docker Image CI Prod

on:
  push:
    branches: [test_stage]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build docker-compose.prod.yml --file Dockerfile.prod --tag my-image-name:$(date +%s)